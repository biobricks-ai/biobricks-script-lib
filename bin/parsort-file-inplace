#!/bin/sh

set -eu

if [ "$#" -ne 1 ] || [ -z "$1" ]; then
	cat <<EOF >&2
Usage:
  $0 file
EOF
	exit 1
fi

DIR=`dirname $1`
TEMPFILE=$(mktemp -p $DIR)
chmod --reference=$1 $TEMPFILE

parsort $1 > $TEMPFILE
mv $TEMPFILE $1
